# كيف تبني ملف APK من الجوال فقط (بدون كمبيوتر)

بما إنه ما عندك كمبيوتر، أسهل طريقة هي إنك تخلي **GitHub** يبني
لك الـ APK "بالسحابة" مجاناً، وكل الخطوات ممكن تسويها من متصفح
الجوال أو تطبيق GitHub.

## الخطوات

### 1) اعمل حساب على GitHub (لو ما عندك)
افتح github.com من متصفح الجوال وسجّل حساب جديد (مجاني).

### 2) أنشئ مستودع (Repository) جديد
- اضغط على "+" ثم "New repository".
- اختر اسم مثل `SwitchController`.
- خله **Public** أو **Private** (كلاهما يشتغل، الـ Actions مجانية للحسابات الشخصية).
- اضغط "Create repository".

### 3) ارفع ملفات المشروع
داخل المستودع الجديد:
- اضغط "Add file" → "Upload files".
- فك ضغط ملف `SwitchController.zip` أولاً (فيه تطبيقات على الجوال
  تفك الضغط زي "Files" على أندرويد أو تطبيقات مخصصة مثل ZArchiver).
- ارفع **كل الملفات والمجلدات** بنفس البنية اللي هي عليها
  (يعني مجلد `app` كامل، ومجلد `.github` كامل، وملفات `build.gradle`
  و`settings.gradle` و`gradle.properties` بالجذر).
- تنبيه: تأكد إن مجلد `.github/workflows/build.yml` انرفع بنفس المسار
  (بعض تطبيقات رفع الملفات على الجوال تخفي المجلدات اللي تبدأ بنقطة `.`
  — إذا صار هيك، أنشئ الملف يدوياً من داخل GitHub: "Add file" → "Create new file"
  واكتب المسار `.github/workflows/build.yml` باسم الملف، والصق فيه نفس المحتوى).

### 4) شغّل عملية البناء (Build)
- روح لتبويب **"Actions"** أعلى صفحة المستودع.
- بتلاقي workflow اسمه **"Build APK"**.
- اضغط عليه، ثم اضغط **"Run workflow"** (زر أخضر) لتشغيله يدوياً.
  (لو رفعت الملفات لأول مرة على فرع main فراح يشتغل تلقائياً كمان).
- انتظر 2-5 دقائق لين تخلص العملية (دائرة خضراء ✅ = نجح، حمراء ❌ = فيه خطأ).

### 5) نزّل الـ APK
- بعد ما تخلص العملية بنجاح، افتح الـ run نفسه.
- تحت بترى قسم **"Artifacts"** فيه ملف اسمه `SwitchController-debug-apk`.
- حمّله (بينزل كملف zip فيه الـ APK بداخله)، فك ضغطه، وبيطلعلك `app-debug.apk`.

### 6) ثبّت التطبيق على جوالك
- افتح ملف `app-debug.apk` من مدير الملفات.
- إذا طلب منك أندرويد تفعيل "السماح بالتثبيت من هذا المصدر"، وافق.
- ثبّت وجرّب.

## ملاحظة مهمة
لو ظهر خطأ (❌) بعملية البناء، افتح تفاصيل الخطأ من صفحة الـ run
(بيوريك بالضبط أي سطر أو ملف فيه مشكلة) وابعتلي نص الخطأ، وبساعدك تصلحه.
